[package]
name = "client-sync"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.56", features = ["derive"] }
tungstenite = { version = "0.28.0", features = ["rustls-tls-native-roots"] }
